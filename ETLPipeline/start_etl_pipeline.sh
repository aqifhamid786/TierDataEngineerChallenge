#!/bin/sh
spark-submit --packages ${SPARK_JOB_DEPENDENCIES}  --class ${SPARK_JOB_MAIN_CLASS} --master ${SPARK_MASTER_URL} --conf spark.executor.memory=${SPARK_JOB_MAX_MEMORY} --conf spark.cores.max=${SPARK_JOB_MAX_CORES} --conf spark.jars.ivy=/opt/bitnami/spark/ivy --deploy-mode client --verbose /opt/bitnami/app/builds/${SPARK_JOB_JAR} -j ${ETL_PIPELINE} ...